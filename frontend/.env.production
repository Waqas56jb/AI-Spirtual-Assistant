# Used by `npm run build` / Vite production mode only.
# Public API (no trailing slash). Dev (`npm run dev`) ignores this and uses the Vite proxy.
VITE_API_URL=https://ai-spirtual-assistant-backend.vercel.app
